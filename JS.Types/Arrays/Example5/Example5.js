// Remove an element from an array

let activities = ['work', 'sleep', 'eat', 'sleep'];

// Remove last element
activities.pop();
console.log(activities);

// Remove last element and store in variable
const removedElement = activities.pop();

console.log(removedElement);
console.log(activities);
